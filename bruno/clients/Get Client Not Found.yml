info:
  name: Get Client Not Found
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/clients/99999"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
