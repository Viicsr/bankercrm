info:
  name: Get client
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/{{version_url}}/clients/{{client_id}}"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
