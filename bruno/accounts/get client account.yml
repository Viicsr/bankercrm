info:
  name: get client account
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/{{version_url}}/clients/1/accounts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
